

describe('the phone bill factory', function() {
    it('should return the amount of calls', function() {
        var myFactory = phoneBillFactory()
        var calls = phoneBillFactory()
        var sms = phoneBillFactory()
        assert.equal('calls', 0)
        assert.equal('sms', 0)
        assert.equal('billtotal', 0)
        assert.equal

    }
)

}

